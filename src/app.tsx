import ReactDOM from "react-dom/client";
// import { DiceBoxApp as App } from "./DiceBoxApp";
import { BabylonApp as App } from "./BabylonApp";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <App />
);
